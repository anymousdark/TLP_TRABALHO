 perfil pe# TODO - Light Glass

- [x] Atualizar `style.css` para que o tema padrão (light) seja um “Light Glass” (gradiente/blur no body + cards transparentes com border/sombra).
- [ ] Ajustar páginas com inline styles (ex.: alerts/caixas com `background: #dcfce7`, `#f0f9ff`, `linear-gradient(...)`) para não “quebrarem” o visual glass (opcional: trocar para classes CSS).
- [ ] Reaplicar blur no conteúdo extra que usa `div style="background: ..."` (caso ainda fique inconsistente).
- [ ] Validar visualmente as páginas principais (index, carrinho, login/cadastro, admin) e corrigir o que faltar.
- [ ] Validar visualmente as páginas principais (index, carrinho, login/cadastro, admin) e corrigir o que faltar.

